/**
 * Полноценная реализация IndexedDB для Node.
 *
 * Нужна, чтобы слой хранения тестировался тем же кодом, что работает
 * в браузере, а не веткой деградации в память. Иначе тесты персистентности
 * проверяли бы Map, а не настоящие транзакции и структурированное
 * клонирование — то есть именно то, где и ломается реальный код.
 */
import 'fake-indexeddb/auto';
