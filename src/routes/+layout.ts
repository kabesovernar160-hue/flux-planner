// Mini App целиком зависит от window.Telegram, серверный рендер здесь
// не даёт ничего, кроме гидратационных расхождений. SEO у TMA нет.
export const ssr = false;

// Пререндер пустой оболочки: приложение раздаётся как статика,
// без Node-сервера на проде.
export const prerender = true;
